var cities =[{"city":"hellooo"},{"city":"ok"},{"city":"ohello"}]
const regex = new RegExp("helloo",'gi');
var a=1;
//  var array= cities.filter(place => {return (place.city.match(regex))});
/*it shows that whenever place is returning something that is not nulll than everything is true so it will not 
filter anything*/
/*so every element of the array comes in the place variable    so if the the function is returning true than the
element that is comming in varaible place will be present in the filtered array*/
 var array= cities.filter(place => {return b=1});
 console.log(array)